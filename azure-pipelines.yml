trigger:
  - main

pool:
  name: 'Self-Hosted'
  demands:
    - agent.name -equals nhk-VMware-Virtual-Platform

steps:
  - script: echo "Running on self-hosted agent" >> /home/azureuser/agent.log
    displayName: 'Hello World'
